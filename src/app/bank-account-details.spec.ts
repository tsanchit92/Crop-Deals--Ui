import { BankAccountDetails } from './bank-account-details';

describe('BankAccountDetails', () => {
  it('should create an instance', () => {
    expect(new BankAccountDetails()).toBeTruthy();
  });
});
